{# <nav class="navbar">
    <div class="nav_logo">
        <a href="{{ path('app_home') }}" class="logo">
            <img src="/images/logo.svg" alt="Logo GreenGoodies">
        </a>
    </div>

    <div class="nav_links">
        {% if app.user %}
            <a href="{{ path('app_product') }}">Nos produits</a>
            <a href="{{ path('app_cart') }}">Mon panier</a>
            <a href="{{ path('app_account') }}">Mon compte</a>
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_login') }}">Connexion</a>
            <a href="{{ path('app_register') }}">Inscription</a>
        {% endif %}
    </div>
</nav> #}


<nav class="navbar">
    <div class="nav_logo">
        <a href="{{ path('app_home') }}" class="logo">
            <img src="/images/logo.svg" alt="Logo GreenGoodies">
        </a>
    </div>

    <!-- Burger -->
    <div class="burger" id="burger">
        <span></span>
        <span></span>
        <span></span>
    </div>

    <div class="nav_links" id="navLinks">
        {% if app.user %}
            <a href="{{ path('app_product') }}">Nos produits</a>
            <a href="{{ path('app_cart') }}">Mon panier</a>
            <a href="{{ path('app_account') }}">Mon compte</a>
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_login') }}">Connexion</a>
            <a href="{{ path('app_register') }}">Inscription</a>
        {% endif %}
    </div>
</nav>